{
  "google-workspace": "[ENCRYPTED — set locally, never sync plaintext]",
  "vercel": "[ENCRYPTED — set locally, never sync plaintext]",
  "post-bridge": "[ENCRYPTED — set locally, never sync plaintext]",
  "descript": "[ENCRYPTED — set locally, never sync plaintext]",
  "quickbooks-online": "[ENCRYPTED — set locally, never sync plaintext]",
  "mit-ai-gateway": "[ENCRYPTED — set locally, never sync plaintext]",
  "firecrawl": "[ENCRYPTED — set locally, never sync plaintext]",
  "slack": "[ENCRYPTED — set locally, never sync plaintext]",
  "godaddy": "[ENCRYPTED — set locally, never sync plaintext]",
  "microsoft-365": "[ENCRYPTED — set locally, never sync plaintext]",
  "supabase": "[ENCRYPTED — set locally, never sync plaintext]",
  "dropbox": "[ENCRYPTED — set locally, never sync plaintext]",
  "higgsfield": "[ENCRYPTED — set locally, never sync plaintext]",
  "viewmax": "[ENCRYPTED — set locally, never sync plaintext]"
}
